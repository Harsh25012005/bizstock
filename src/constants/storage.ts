export const STORAGE_KEYS = {
  auth: "bizstock.auth",
  app: "bizstock.app",
  order: "bizstock.order",
  product: "bizstock.product",
  syncQueue: "bizstock.sync.queue",
  secureAccessToken: "bizstock.secure.access-token",
} as const;
